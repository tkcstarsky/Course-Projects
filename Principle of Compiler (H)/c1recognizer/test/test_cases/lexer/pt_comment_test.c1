/\
/ This program is used for tesing comment. 
float a[5]; //define an array
int main()
{
    int i = 1;
    while (i > 0)
    {
        if (i < 5)
        a[i - 1] = i;
        else i = 0;
        //i < 5 : a[i-1]<-i  \
          i = 5 : break
        i++;
    } 
    /*test 
    test1
    test2
    test3 */a[0] = 0;
}
